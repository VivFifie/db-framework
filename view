#create a view that references tbl_transactions table:

CREATE OR REPLACE VIEW public.vw_transactions
 AS
select transaction_id,recorded_at, matched, company_id,other_transaction_id from tbl_transactions;
